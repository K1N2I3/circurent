# CircuRent Environment Variables Template
# 复制此文件为 .env.local 并填写实际值
# 部署到 Vercel 时，在项目设置中添加这些环境变量

# ============================================
# Supabase Database (必需)
# ============================================
# 获取方式: https://app.supabase.com → 项目 → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# JWT Authentication (必需)
# ============================================
# 生成方式: openssl rand -base64 32
# 或: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# ============================================
# Resend Email Service (必需)
# ============================================
# 获取方式: https://resend.com/api-keys
# 注意: onboarding@resend.dev 只能发送到注册邮箱
# 要发送到所有邮箱，需要验证域名: https://resend.com/domains
RESEND_API_KEY=re_xxxxxxxxxxxxx
RESEND_FROM_EMAIL=CircuRent <onboarding@resend.dev>

# ============================================
# Google Maps (可选)
# ============================================
# 如果未配置，将使用免费的 OpenStreetMap
# 获取方式: https://console.cloud.google.com
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ============================================
# PayPal (可选 - 用于支付)
# ============================================
NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_paypal_client_id

# ============================================
# Stripe (可选 - 用于支付)
# ============================================
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key

